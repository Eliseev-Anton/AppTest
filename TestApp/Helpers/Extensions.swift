import UIKit

extension UIApplication {
    // Note: deprecated in iOS 13+, kept simple for demo. In production prefer alternative UI.
    var isNetworkActivityIndicatorVisible: Bool {
        get { return false }
        set {  }
    }
}
